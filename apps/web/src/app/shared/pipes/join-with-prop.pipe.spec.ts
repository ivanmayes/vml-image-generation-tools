import { JoinWithPropPipe } from './join-with-prop.pipe';

describe('JoinWithPropPipe', () => {
	it('create an instance', () => {
		const pipe = new JoinWithPropPipe();
		expect(pipe).toBeTruthy();
	});
});
